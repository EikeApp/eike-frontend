name: feat_notification
description: This package contains a notification system.
publish_to: none
version: 0.0.1 # Literally does not matter
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ^3.41.2

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  flutter_bloc:
  bloc_concurrency:
  bloc_presentation:
  use_in_case:
  dartz:

  service_logging:
  service_design:
dev_dependencies:
  build_runner:
  freezed:
