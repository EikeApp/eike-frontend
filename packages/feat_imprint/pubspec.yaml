name: feat_imprint
description: This package contains contents related to the imprint screen.
publish_to: none
version: 0.0.1 # Literally does not matter
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ^3.44.2

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  flutter_bloc:
  bloc_concurrency:
  use_in_case:
  dartz:
  rxdart:
  drift:
  meta:
  flutter_html:

  data_database:
  data_entities:
  service_design:
  service_logging:
  service_settings:
  service_url_launcher:

dev_dependencies:
  test:
  build_runner:
  freezed:
  drift_dev:
  rx_shared_preferences:
