name: feat_contact
description: This package contains contents related to the contact screen.
publish_to: none
version: 0.0.1 # Literally does not matter
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ^3.41.2

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  flutter_bloc:
  bloc_concurrency:
  use_in_case:
  dartz:
  drift:
  meta:

  data_database:
  data_entities:
  service_design:
  service_logging:
  service_url_launcher:

dev_dependencies:
  test:
  build_runner:
  drift_dev:
  freezed:
  json_serializable:
